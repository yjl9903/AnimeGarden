import type { FetchedResource } from 'animegarden';

import type { Database } from '../connection';

export async function insertDmhyResources(database: Database, resources: FetchedResource[]) {
  // TODO
}
