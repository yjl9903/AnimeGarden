import type { AnitomyOptions, ParsedResult } from './types';

export function tokenize(filename: string, result: ParsedResult, options: AnitomyOptions) {
  const tokens: Token[] = [];
  return tokens;
}

export class Token {}
