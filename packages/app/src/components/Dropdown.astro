---
export interface Props {
  className?: string;
}

const { className = '' } = Astro.props;
---

<div class={`relative z10 [&:hover>.c-dropdown]:block ${className}`}>
  <div><slot /></div>
  <div class="c-dropdown hidden absolute top-full">
    <slot name="dropdown" />
  </div>
</div>
