{
  "name": "@animegarden/worker",
  "version": "0.5.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest",
    "test:ci": "wrangler deploy --dry-run && vitest --run"
  },
  "dependencies": {
    "@animegarden/client": "workspace:*",
    "@animegarden/scraper": "workspace:*",
    "hono": "^4.7.6"
  },
  "devDependencies": {
    "@total-typescript/ts-reset": "^0.6.1"
  }
}
