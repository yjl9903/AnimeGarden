# TanStack Start 迁移对齐测试路径

检查日期：2026-06-25

目标：确认 `http://localhost:5173/` 与线上 `https://animes.garden/` 的页面结构、核心交互和错误表现基本一致，并沉淀后续自动化测试路径。

## 结论摘要

- 首页、资源列表、动画周历、API 文档、移动端 Header/侧栏的主体结构与线上基本一致。
- 详情页直开内容基本一致：标题、下载链接、磁链、原链接、发布者/字幕组、文件列表都能渲染。
- 本地存在迁移风险点，需要优先修复或确认：
  - 多页面出现 React hydration mismatch，差异点指向 `#animegarden-progress`。
  - 本地 `/resources/2?type=动画&type=合集&preset=bangumi` 为空，线上同路径有资源。
  - 本地搜索 `葬送的芙莉莲` 后进入 `/resources/1?search=...`，主线程复测为 0 条；线上有 30 条。
  - 本地列表磁链缺 tracker；线上同类磁链包含 `&tr=`。
  - 本地从首页点击详情链接、详情页点击外链存在回到首页或不跳转的报告，需要单独复现定位。

## 自动化测试路径

### 首页

路径：`/`

断言：

- 页面 title 为 `Anime Garden 動漫花園資源網镜像站 动漫花园动画 BT 资源聚合站`。
- Header 存在 `动画`、`字幕组`、`资源`。
- 首页 Header RSS 可见，`href` 为 `https://api.animes.garden/feed.xml`。
- 资源表头包含 `资源`、`发布者`、`播放`。
- 资源行数为 30。
- 前几条资源行包含标题、发布时间、大小、详情、发布者/字幕组。
- 每条资源有播放链接和 `magnet:` 下载链接。
- `magnet:` 下载链接在 hydration 后包含 tracker `&tr=`。
- 浏览器 console 不应出现 `Hydration failed`。

### 首页分页

路径：`/`，操作：点击 `下一页`。

等价直达路径：`/resources/2?type=动画&type=合集&preset=bangumi`

断言：

- URL 进入 `/resources/2?type=动画&type=合集&preset=bangumi`。
- 不出现 `没有搜索到匹配的资源`。
- 资源行数为 30。
- 分页包含上一页和下一页。

### 全部资源

路径：`/resources/1`

断言：

- title 包含 `所有资源`。
- 资源行数为 30。
- RSS 链接为 `https://api.animes.garden/feed.xml`。
- 点击首个可见 `/detail/:provider/:providerId` 链接后，URL 进入详情页。
- 执行浏览器 back 后回到 `/resources/1`。

### 筛选资源

路径：`/resources/1?type=动画&preset=bangumi`

断言：

- title 包含 `最新动画资源`。
- 搜索框值为 `类型:动画 预设:番剧`。
- 资源行数为 30。
- RSS 链接保留 `type=动画&preset=bangumi` 查询参数。

路径：`/resources/1?fansub=ANi`

断言：

- 资源行数为 30。
- 前几条资源的发布者/字幕组均为 `ANi`。

### 搜索

路径：`/`，操作：聚焦搜索框，输入 `葬送的芙莉莲`。

断言：

- 搜索弹层出现 `动画`、`搜索结果`、`高级搜索` 分组。
- 建议项包含 `葬送的芙莉莲` 或 `葬送的芙莉莲 第二季`。
- 提交“在本页列出...”后，URL 为 `/resources/1?search=%E8%91%AC%E9%80%81%E7%9A%84%E8%8A%99%E8%8E%89%E8%8E%B2`。
- 页面不显示 `发生错误`。
- 资源行数大于 0。
- 搜索框保留输入值。

### 详情页

路径：`/detail/dmhy/721556`

断言：

- `h1` 显示资源标题。
- 页面包含 `下载链接`、`发布者` 或 `发布者 / 字幕组`、`文件列表`。
- 磁链 `href` 以 `magnet:` 开头，并包含 tracker。
- 原链接可见，且 `target="_blank"`。
- PikPak 播放链接可见。
- 点击 `下载链接`、`原链接` 后，当前应用路由不应变成 `/`。

路径：`/detail/dmhy/721555`

断言：

- 页面允许显示 `种子信息解析失败`。
- 即使文件解析失败，磁链和原链接仍可见。

首页进入详情：

- 从 `/` 点击首个可见 `/detail/...` 链接。
- 断言 URL 变为 `/detail/:provider/:providerId`。
- 浏览器 back 后回到 `/`。

### 动画周历

路径：`/anime`

断言：

- title 包含 `动画周历`。
- 页面存在 `星期一` 到 `星期日` 中的周历分组。
- 每个可见动画卡片包含海报图和跳转到 `/subject/:id` 的链接。
- 横向滚动按钮点击后列表滚动位置变化。

### 静态页和 API 文档

路径：`/about`

断言：

- title 包含 `关于`。
- Header 存在。
- 当前线上与本地页面主体都为空；如果后续补内容，再追加内容断言。

路径：`/docs/api`

断言：

- title 包含 `API 文档`。
- Swagger UI 渲染。
- 页面包含 `🌸 Anime Garden API`、`Resources`、`GET /resources`。

### 移动端

视口：`390x844`

路径：`/`

断言：

- Header 显示 `🌸`、`动画`、`字幕组`、`资源`。
- Header 不显示 RSS。
- 搜索框可见。
- 资源行数为 30。
- 页面没有明显横向溢出。
- 打开侧栏后出现 `动画周历`、`所有资源`、`高级搜索帮助`、`收藏夹`。

### 主题

路径：任意 Layout 页面，例如 `/`

断言：

- 点击暗色主题后，`html` 元素包含 `dark` class。
- 点击亮色主题后，`html` 元素移除 `dark` class。
- 主题按钮应有可访问名称；当前建议补 `aria-label` 后再写强断言。

### 通配路径

路径：`/__codex_missing_404__`

当前线上行为：重定向到 `/`。

断言：

- 如果保持线上兼容，断言最终 URL 为 `/`，首页资源表渲染。
- 如果迁移目标改为 404，先改产品行为，再断言显示 404/Not Found。

## 对比时避免脆弱断言

- 线上资源数据实时变化，不要断言固定首条标题，除非使用固定测试数据或 mock API。
- 可以断言资源条数、字段结构、链接类型、URL 参数、错误文案是否出现。
- `dmhy/721556`、`dmhy/721555` 可作为短期详情页样本；长期自动化应从列表动态取前 1 条详情链接。
